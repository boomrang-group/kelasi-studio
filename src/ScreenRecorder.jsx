import React from 'react';

const ScreenRecorder = () => {
  return (
    <div className="recorder-placeholder screen-recorder">
      Screen Recording Interface - Options for screen selection (full, window, tab) will be here. Preview will show here.
    </div>
  );
};

export default ScreenRecorder;
